import { workflow } from "@/lib/network-data";
const roles=["National Editor","State Editor","LGA Subeditor","Issue Subeditor","Verification Editor","Legal/Risk Reviewer","Corrections Editor","Contributor"];
export default function EditorialModel(){return <main className="section"><div className="container"><div className="section-title"><div className="kicker">Editorial Model</div><h2>Controlled record movement</h2><p>Contributor submits record → LGA or issue desk review → verification review → sensitivity review where necessary → state editor approval → publication → correction channel remains open.</p></div><div className="workflow-line">{workflow.map(w=><div className="workflow-step" key={w}>{w}</div>)}</div><section className="section"><div className="grid four">{roles.map(r=><article className="card" key={r}><h3>{r}</h3><p>Role definition placeholder for a national civic records network.</p></article>)}</div></section></div></main>}

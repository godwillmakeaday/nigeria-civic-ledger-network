import Link from "next/link";
import { stateNodes } from "@/lib/network-data";
export default function StatesPage(){return <main className="section"><div className="container"><div className="section-title"><div className="kicker">State Nodes</div><h2>36 states and FCT network map</h2><p>Only Nasarawa Heartland is marked active. Other states are proposed nodes awaiting local editorial structure.</p></div><div className="grid three">{stateNodes.map(s=><article className="card" key={s.slug}><span className={s.active?"node-status active":"node-status"}>{s.status}</span><h3>{s.name}</h3><div className="meta"><b>Desk</b><span>{s.desk}</span><b>Region</b><span>{s.region}</span><b>Readiness</b><span>{s.readiness}</span></div><Link className="card-link" href={`/network/states/${s.slug}`}>Open state node →</Link></article>)}</div></div></main>}

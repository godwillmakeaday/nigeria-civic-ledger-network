import { replication } from "@/lib/network-data";
export default function Replicate(){return <main className="section"><div className="container"><div className="section-title"><div className="kicker">Replicate</div><h2>Prepare a state civic node</h2><p>This page explains how to replicate Nasarawa Heartland for another state without inventing records or building a partisan platform.</p></div><div className="grid two">{replication.map(r=><article className="card" key={r}><h3>{r}</h3><p>Required before a state node becomes credible, safe, and editorially disciplined.</p></article>)}</div><div className="cta-row"><button className="btn light" type="button">Request State Node Setup</button><button className="btn light" type="button">Start with Public Prototype</button><button className="btn light" type="button">Build Editorial Desk</button></div></div></main>}
